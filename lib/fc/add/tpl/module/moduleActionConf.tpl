/**
 * @file ${desc} - Action配置
 *
 * @author ${author}(${email})
 * @date ${date}
 */

define(function (require) {
    var confList = [
        {
            path: '/${moduleName}',
            type: '${moduleName}/Action'
        }
    ];

    return confList;
});
