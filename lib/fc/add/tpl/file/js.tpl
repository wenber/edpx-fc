/**
 * @file ${desc}
 *
 * @author ${author}(${email})
 */

define(function (require) {
    /**
     * ${desc}
     */
    var ${fileName} = function () {
        // do something here
    }

    return ${fileName};
});
