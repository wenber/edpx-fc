/**
 * @file ${desc}
 *
 * @author ${author}(${email})
 * @date ${date}
 */

define(function (require) {
    /**
     * ${desc}
     */
    var ${fileName} = function () {
        // do something here
    }

    return ${fileName};
});
