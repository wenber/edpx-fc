/**
 * @file ${desc} - Action配置
 *
 * @author ${author}(${email})
 */
define(function (require) {
    var confList = [
        {
            type: '/${moduleName}',
            path: '${moduleName}/Action'
        }
    ];

    return confList;
});
